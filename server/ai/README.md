# ai/ —— AI 人格模块（Phase 3）

按设计文档 §5 规划：

- `persona.js` —— 小伴人格提示词（观察者 + 轻度社恐 + 偶尔玩梗），语气范例取自设计文档
- `memory.js` —— 对话记忆与用户特征存档（支撑"特征总结与开解"）
- `client.js` —— 大模型调用封装（后续接入真实 API；离线模式下降级为模板话术）

原则：AI 输出**只影响聊天内容与彩蛋奖励**，主积分永远由 services/ 的确定性规则计算，保证分数可信。
